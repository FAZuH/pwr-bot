[package]
name = "pwr-plugin-support"
version = "0.1.0"
edition = "2024"
authors = ["FAZuH <mail@fazuh.com>"]
description = "Shared plugin-side plumbing for pwr-bot panel plugins: session state, host-call bookkeeping, wire writing, and settings return"
license = "MIT"
repository = "https://github.com/FAZuH/pwr-bot"

[dependencies]
pwr-plugin-protocol = { path = "../../pwr-plugin-protocol" }
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.149"
