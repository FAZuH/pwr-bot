[package]
name = "pwr-bot-sdk"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "SDK for building pwr-bot plugins"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tokio = { version = "1", features = ["rt"] }
async-trait = "0.1"
