[package]
name = "pwr-bot-plugin-feed"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Feed subscription management plugin for pwr-bot"

[dependencies]
pwr-bot-sdk = { path = "../pwr_bot_sdk" }
async-trait = "0.1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
